# core/

앱 전역에서 쓰이는 공통 인프라. 특정 feature에 종속되지 않는다.

- `theme/`: 색상, 텍스트 스타일, ThemeData
- `network/`: Dio 클라이언트, API 엔드포인트
- `storage/`: SecureStorage 래퍼 (JWT 등)
- `constants/`: 앱 전역 상수
- `utils/`: validators 등 유틸 함수
